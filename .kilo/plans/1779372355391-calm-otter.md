# Plan: Reduce Hero Section Images + Add Bottom Margin

## Goal
Make images inside the **hero section** (`event-detail.html` — `<section class="event-detail-hero">`) smaller, and add `margin-bottom` spacing between the text column and the image column.

## Scope
- User explicitly said: **"hero section only"**
- Affects two css classes: `.hero-img` and `.hero-img--detail` in `event-detail.css`

---

## Files to Edit

### `event-detail.css` — hero image selectors only

| Selector | Change | Reason |
|---|---|---|
| `.hero-img` | `max-width`: `340px` → **`200px`**; `margin-bottom: 16px` | Smaller in hero |
| `.hero-img--detail` | `max-width`: `420px` → **`260px`**; `margin-bottom: 16px` | Keep the detail variant in proportion |
| `.hero-images-wrap, `.hero-images-wrap--detail` | Adjust inner horizontal padding / max-width proportionally | Prevent the smaller image from feeling cramped |

All other styles (border-radius, box-shadow, object-fit) are kept identical.

---

## Notes
- `ng build --configuration development` will be run to confirm no compile errors after changes.
- No actual image files are resized — changes are CSS-only.
